import { PersonaCoordinadora } from './persona-coordinadora';

describe('PersonaCoordinadora', () => {
  it('should create an instance', () => {
    expect(new PersonaCoordinadora()).toBeTruthy();
  });
});
